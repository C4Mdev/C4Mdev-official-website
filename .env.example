# Contact form → Web3Forms (https://web3forms.com)
# Create a form, copy the Access Key, paste below, save as .env.local
#
# Must be NEXT_PUBLIC_* so the browser can submit (Web3Forms free tier; server-side needs paid + IP safelist).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=your_access_key_here
