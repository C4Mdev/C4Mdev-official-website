# Smart Laser — images

Used by the **Smart Laser** project (`slug: smart-laser`) in **`src/data/portfolioProjects.ts`**.

| File | Use |
|------|-----|
| `hero.jpg` or `hero.png` | Main project image (listing + detail hero) |
| `gallery-1.jpg` … `gallery-3.jpg` | Gallery thumbnails |

Update paths in **`portfolioProjects.ts`** when you add real files (replace `placeholder.svg`).

Until then, the site uses **`placeholder.svg`**.
